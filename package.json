{
  "name": "intel-security-platform",
  "version": "1.0.0",
  "description": "INTEL — Pentest & Security Auditing Platform",
  "scripts": {
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "cd web-ui && npm install && npx vite build",
    "build:backend": "cd backend && npm install && npx tsc --noEmit false --outDir dist || true",
    "start": "node backend/dist/index.js",
    "start:dev": "npx tsx backend/src/index.ts",
    "install:all": "npm install && cd web-ui && npm install && cd ../backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
